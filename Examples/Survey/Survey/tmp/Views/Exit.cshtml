@model VoiceModel.Exit
           
@{
    Layout = "~/tmp/Views/_Layout.cshtml";
}

<form id="@Model.id">
  <block>
    @{Html.RenderPartial("Prompt", Model.ExitPrompt);}
    <exit />

  </block>
</form>
