@model List<VoiceModel.Prompt>

  @foreach (var prompt in @Model)
  {
      <prompt>
        @foreach (var audio in prompt.audios)
        {
            @Html.Raw(audio.Render());
        }
      </prompt>
  }
